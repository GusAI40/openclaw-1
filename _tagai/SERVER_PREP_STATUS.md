# Server Prep Status — 2026-04-26 — REVISED

## Initial mental model was wrong

The first run of this prep treated the server as a greenfield deployment and wrote a fresh `~/.tagai-env` from `_tagai/.env.tagai.example`. That was incorrect.

A parallel preflight agent caught it: OpenClaw is **already deployed** on `tagai-cloud` via plain `docker compose` at `/home/tagai/openclaw/`. The gateway container had been running 17 hours healthy on port 18789 using the original `.tagai-env`. The CLI container is the unhealthy one.

The server runs **Caddy on the host** (not Traefik+Coolify), so the `_tagai/docker-compose.tagai.yml` overlay was not in use.

## Action taken (rollback)

- Generated a token initially: YES — written into the overwritten `.tagai-env`. The token has been **discarded** (file restored from backup).
- Backed up existing `.tagai-env`: YES — `~/.tagai-env.bak.20260426-174025` (2901 bytes) was created before the original overwrite.
- Created any new dirs: YES — `~/.openclaw/agents/main/sessions` was added to the existing `~/.openclaw/` tree. Nothing destructive; the new empty subdir is harmless.
- Restoration performed: YES — `cp ~/.tagai-env.bak.20260426-174025 ~/.tagai-env && chmod 600 ~/.tagai-env`. Verified the active `.tagai-env` is byte-identical to the pre-task backup (37 lines, 2901 bytes, starts with the `# ── VAPI ──` comment block, NOT the binary token-first variant the overwrite produced).
- The replaced binary `.tagai-env` (39 lines, 1617 bytes, generated by this task) is gone — the restore overwrote it. No further cleanup needed.

## Current server state (verified post-restore)

- `/home/tagai/.tagai-env`: exists, 37 lines, 2901 bytes, chmod 600, contains the original VAPI / OpenClaw / etc. block. `grep -c '^OPENCLAW_'` returns 1 (matches pre-task state).
- `/home/tagai/.openclaw/`: exists, chmod 750, owner `tagai:tagai`. Contains the live state from prior sessions: `agents/`, `canvas/`, `credentials/`, `devices/`, `extensions/`, `identity/`, `jarvis-vapi/`, `logs/`, `maya-vapi/`, `memory/`, `openclaw.json` (+ 5 backups + 1 `.last-good`), `plugin-runtime-deps/`, `tasks/`, `telegram/`, `update-check.json`, `workspace/`, `exec-approvals.json`. Plus the new empty `agents/main/sessions/` subdir from the initial run (harmless).
- `/home/tagai/.tagai-env.bak.20260426-174025`: retained as proof of restoration.
- Running containers:
  ```
  openclaw-openclaw-cli-1       Up 17 hours (unhealthy)
  openclaw-openclaw-gateway-1   Up 2 minutes (healthy)
  ```
  Gateway restarted ~2 min ago (likely a healthcheck failure during the window when the overwritten token was active). Now healthy after restore.

## Outstanding for next phase

- **CLI container unhealthy** — separate atom needed. Investigate `docker logs openclaw-openclaw-cli-1` and the healthcheck definition in the on-server compose file.
- **`_tagai/docker-compose.tagai.yml` is invalid for this env** — it assumes Traefik/Coolify but the server uses host-level Caddy. Needs a rewrite (or removal) to match reality.
- **Repo reconciliation** — the on-server `/home/tagai/openclaw/` deployment vs our fork `GusAI40/openclaw-1`. Determine whether the server tracks our fork, upstream, or a divergent local copy. Decide canonical source before the next deploy.
- **Documentation correction** — `_tagai/CLAUDE.md` and `_tagai/DEPLOY_HETZNER.md` reference Coolify; they should be updated to reflect the actual Caddy + plain `docker compose` topology.
- **Credentials inventory** — the original `.tagai-env` likely already contains live `CLAUDE_AI_SESSION_KEY` / `CLAUDE_WEB_*` and other secrets. No need to re-run `openclaw onboard` unless something is missing.

## Note on the `agents/main/sessions/` dir

The initial run created `~/.openclaw/agents/main/sessions/` as an empty subdir. It was NOT deleted during rollback because (a) it is empty and inert, (b) deleting it now risks racing with the live container if it scans that path. Safe to leave; remove later if desired.
