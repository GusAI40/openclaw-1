# OpenClaw Health Probe — 2026-04-27

## TL;DR
- Verdict: YELLOW
- /healthz: 200 in 0.844s, body match: yes
- TLS: expires 2026-05-27 (30 days)
- Latency p95 across 2 probes: 0.844s

## Probes
| Probe | Status | Latency | Detail |
|---|---|---|---|
| /healthz | 200 | 0.844s | body matches expected |
| / | 200 | 0.105s | — |
| TLS | valid | — | notAfter=2026-05-27 (30 days) |

## Anomalies
- TLS certificate expires in exactly 30 days (2026-05-27 02:58:02 UTC). GREEN threshold requires >30 days; renewal recommended promptly.
- Note: an earlier probe run today recorded HTTP 503 / "DNS cache overflow" on both endpoints (RED). Current probe shows service recovered (HTTP 200). Intermittent instability possible.

## Raw outputs

```
# /healthz probe
$ curl --max-time 10 -s -w '\nHTTP=%{http_code} latency=%{time_total}s\n' https://openclaw.ubntag.com/healthz
{"ok":true,"status":"live"}
HTTP=200 latency=0.843556s

# / probe
$ curl --max-time 10 -s -w '\nHTTP=%{http_code} latency=%{time_total}s\n' https://openclaw.ubntag.com/
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>OpenClaw Control</title>
    ...
  </head>
  <body><openclaw-app></openclaw-app></body>
</html>
HTTP=200 latency=0.104683s

# TLS cert check
$ echo | openssl s_client -servername openclaw.ubntag.com -connect openclaw.ubntag.com:443 2>/dev/null \
    | openssl x509 -noout -dates
notBefore=Apr 27 02:58:03 2026 GMT
notAfter=May 27 02:58:02 2026 GMT

# Earlier probe (from remote, same date) recorded:
# HTTP=503 latency=0.541792s — body: "DNS cache overflow"
```
