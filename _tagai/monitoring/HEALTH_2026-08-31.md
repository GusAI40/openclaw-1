# OpenClaw Health Probe — 2026-08-31

## TL;DR
- Verdict: YELLOW
- /healthz: 200 in 1.012s, body match: yes
- TLS: expires 2026-09-30 (30 days) — at threshold, GREEN requires >30 days
- Latency p95 across 2 probes: 1.012s

## Probes
| Probe | Status | Latency | Detail |
|---|---|---|---|
| /healthz | 200 | 1.012s | body matches expected `{"ok":true,"status":"live"}` |
| / | 200 | 0.472s | HTML response, OpenClaw Control UI |
| TLS | valid | — | notAfter=2026-09-30 (30 days) |

## Anomalies
- TLS certificate expires in exactly 30 days (2026-09-30). GREEN threshold is >30 days remaining. Renewal recommended soon to avoid entering the RED zone.

## Raw outputs

```
# Probe 1: /healthz
$ curl --max-time 10 -s -w '\nHTTP=%{http_code} latency=%{time_total}s\n' https://openclaw.ubntag.com/healthz
{"ok":true,"status":"live"}
HTTP=200 latency=1.012099s
```

```
# Probe 2: /
$ curl --max-time 10 -s -w '\nHTTP=%{http_code} latency=%{time_total}s\n' https://openclaw.ubntag.com/
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    ...
    <title>OpenClaw Control</title>
    ...
  </head>
  <body>
    <openclaw-app></openclaw-app>
  </body>
</html>
HTTP=200 latency=0.471528s
```

```
# Probe 3: TLS cert
$ echo | openssl s_client -servername openclaw.ubntag.com -connect openclaw.ubntag.com:443 2>/dev/null \
  | openssl x509 -noout -dates
notBefore=Aug 31 14:23:41 2026 GMT
notAfter=Sep 30 14:24:41 2026 GMT
```
